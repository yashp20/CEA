name = "cea-proxy"
main = "worker.js"
compatibility_date = "2026-06-01"

# Secrets (never in this file):
#   npx wrangler secret put ANTHROPIC_API_KEY
#   npx wrangler secret put SUPERMEMORY_API_KEY   # v1.1 §5 (TODO(cea): set)
# Local dev: put ANTHROPIC_API_KEY in proxy/.dev.vars (gitignored), then
#   npx wrangler dev

# v1.1 §3.1 — anonymous per-venue accessibility reports.
[[kv_namespaces]]
binding = "CEA_REPORTS"
id = "f46d4472004d45108f44db0b69346134"
